#include "monty.h"
/**
 * f_stack - sets the stack mode/prints from the top
 * @head: pointer to the head of the stack
 * @linenumber: line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int linenumber)
{
	(void)head;
	(void)linenumber;
	bus.lifi = 0;
}
